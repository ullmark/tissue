
// Views: Start
// ------------
// This View handles the initial state of the application
// when the user isn't authenticated or doesn't have a valid
// access token

module.exports = Backbone.View.extend({
  el: $('#start')
});
